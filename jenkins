node {
     stage('step into day-1'){
	sh "cd day-1-ec2-create"
     }

    stage('init') {
	 sh 'terraform init'
    }  
    stage('plan') {		
	 sh 'terraform plan'
    } 
   stage('choice') {
	sh 'terraform ${action} --auto-approve'	
    } 
}
